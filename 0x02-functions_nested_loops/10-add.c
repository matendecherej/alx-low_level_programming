#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: first number to compute
 * @b: second number to compute
 *
 * Return: Integer value
 */
int add(int a, int b)
{
	return (a + b);
}
